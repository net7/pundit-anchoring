export declare type Selector = {
    start: number;
    end: number;
};
export declare type TextPosition = {
    start: number;
    end: number;
};
export declare type TextQuoteSelector = {
    exact: string;
    prefix?: string;
    suffix?: string;
};
