export type Selector = {
    start: number;
    end: number;
};
export type TextPosition = {
    start: number;
    end: number;
};
export type TextQuoteSelector = {
    exact: string;
    prefix?: string;
    suffix?: string;
};
